import React from 'react';
import styled from 'styled-components';


const UnstyledMusings = ({ className }) => (
    <div className={className}>
        MUSINGS PAGE
    </div>
);

const Musings = styled(UnstyledMusings)`
`;

export default Musings;
  